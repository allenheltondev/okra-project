{
  "name": "okra-project",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "npm run --workspaces lint",
    "test": "npm run --workspaces test",
    "build": "npm run --workspaces build",
    "typecheck": "npm run --workspaces typecheck",
    "test:integration": "npm run --workspace okra-backend test:integration"
  }
}
